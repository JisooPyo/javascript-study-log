/**
 * ============================================================
 * MPA와 SPA
 * ============================================================
 *
 * 웹 애플리케이션은 페이지를 어떻게 구성하고, 화면 전환을 어떻게 처리하느냐에 따라 크게 MPA와 SPA 방식으로 나눌 수 있다.
 */

/** ------------------------------------------------------------
 * 1. MPA (Multi Page Application)
 * ------------------------------------------------------------
 *
 * [동작 방식]
 * - 사용자가 웹 사이트에 접속하여 panda.html을 요청한다.
 * - 서버는 panda.html 파일을 브라우저에 전달한다.
 * - 브라우저는 서버로부터 전달받은 HTML 파일을 렌더링한다.
 * - 사용자는 해당 페이지 화면을 확인할 수 있다.
 *
 * 이처럼
 * - 서버가 여러 개의 HTML 페이지를 가지고 있고
 * - 페이지 이동 시마다 새로운 HTML 파일을 요청하는 구조를
 *   MPA(Multi Page Application)라고 한다.
 *
 * ------------------------------------------------------------
 * 렌더링 방식
 * ------------------------------------------------------------
 *
 * - MPA는 서버에서 HTML을 만들어 전달하는 방식이므로 서버 사이드 렌더링(Server Side Rendering, SSR) 구조이다.
 *
 * ------------------------------------------------------------
 * MPA의 특징
 * ------------------------------------------------------------
 *
 * [장점]
 * - 첫 페이지 로딩 시간이 비교적 빠르다.
 * - 검색 엔진 최적화(SEO)에 유리하다.
 *
 * [단점]
 * - 페이지 이동 속도가 느리다.
 * - 페이지 이동 시 화면이 깜빡이는 현상이 발생한다.
 * - 페이지 이동 시 모든 요소가 다시 렌더링된다.
 * - 동일한 레이아웃이나 로직을 여러 HTML 파일에 중복해서 작성해야 한다.
 * - 서버에 부하가 걸리기 쉽다.
 * - 사용자 경험(UX)이 좋지 않다.
 */

/** ------------------------------------------------------------
 * 2. SPA (Single Page Application)
 * ------------------------------------------------------------
 *
 * MPA의 단점을 개선하기 위해 다음 단계의 웹 애플리케이션은 SPA 방식으로 개발되었다.
 *
 * ------------------------------------------------------------
 * SPA의 동작 방식
 * ------------------------------------------------------------
 *
 * - 사용자가 웹 사이트에 접속한다.
 * - 서버는 panda.html 같은 개별 페이지가 아니라 하나의 index.html 파일을 전달한다.
 * - index.html에는 자바스크립트 파일이 명시되어 있고, 브라우저는 이 자바스크립트 파일(예: App.js)을 다운로드한다.
 * - 브라우저는 다운로드한 자바스크립트 파일을 실행하고, 자바스크립트 코드가 컴포넌트들을 렌더링하여 화면을 구성한다.
 *
 * ------------------------------------------------------------
 * 렌더링 방식
 * ------------------------------------------------------------
 *
 * - 화면 렌더링을 브라우저(클라이언트)가 직접 수행하므로 클라이언트 사이드 렌더링(Client Side Rendering, CSR) 구조이다.
 *
 * ------------------------------------------------------------
 * SPA의 특징
 * ------------------------------------------------------------
 *
 * [장점]
 * - 페이지 이동 시 전체 페이지를 다시 로드하지 않는다.
 * - 화면이 깜빡이지 않는다.
 * - 변화가 필요한 요소만 선택적으로 변경한다.
 * - 사용자 경험(UX)이 좋다.
 * - 서버 부하가 감소한다.
 *
 * [단점]
 * - 검색 엔진 최적화(SEO)에 불리하다.
 * - 초기 로딩 시간이 길어질 수 있다.(자바스크립트 파일 크기가 크기 때문에 다운로드 및 실행에 시간이 걸릴 수 있음)
 */
